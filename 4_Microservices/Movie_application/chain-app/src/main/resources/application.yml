server:
  port: 3001

management:
  endpoints:
    metrics:
      enabled: true
    web:
      exposure:
        include: "*"
    prometheus:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true